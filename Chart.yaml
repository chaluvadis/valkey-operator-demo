apiVersion: v2
name: valkey-infra
description: Umbrella chart for Valkey platform (operator + cluster) - Production-grade orchestration layer
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: valkey-operator
    alias: operator
    version: 1.0.0
    repository: "file://charts/valkey-operator"

  - name: valkey-cluster
    alias: cluster
    version: 1.0.0
    repository: "file://charts/valkey-cluster"
